demo: &demo
  docker:
    - image: circleci/node:8.11.3

version: 2
jobs:
  build:
    <<: *demo
    steps:
      - checkout
      - run: npm install
      - run: npm run test
      - run: npm run build
      - persist_to_workspace:
          root: .
          paths:
            - .
workflows:
  version: 2
  build-deploy:
    jobs:
      - store_test_results:
        path: tesena-ci-2019/results
      - build:
          filters:
            branches:
              only: master
